@model System.Collections.Generic.IEnumerable<myCAM.Models.GalleryViewModel>

@foreach (var galleryViewModel in Model)
{
    @galleryViewModel.Title
    foreach (var galleryItemInfo in galleryViewModel.Items)
    {
        <img src="@galleryItemInfo.GoodItemData.ThumbnailUrl" />
    }
}